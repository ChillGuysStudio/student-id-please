# Copy to ignored .env. Fill every blank with a distinct URL-safe random value.
# These values configure the Player/Session portion of the shared deployment.
SERVICE_VERSION=1.0.0-rc.2
PLAYER_DB_PASSWORD=
SESSION_DB_PASSWORD=
RABBITMQ_PASSWORD=
SESSION_SERVICE_TOKEN=
MODERATION_SERVICE_TOKEN=
